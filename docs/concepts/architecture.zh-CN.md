# 架构说明

这个公开仓库采用：

**共享核心 + 平台模板 + 安装器**

的结构。

## 分层

- `core/`：跨平台不变的角色、流程、nightly 协议
- `templates/`：各平台真正可复制进项目的 starter
- `installers/`：把模板铺到目标项目目录
- `smoke-tests/`：安装完成后的第一轮验证

## 设计原则

- 共享核心只维护一份
- 平台适配层尽量薄
- 项目级模板优先于机器级私有配置
- 夜间任务必须 task-file 驱动
