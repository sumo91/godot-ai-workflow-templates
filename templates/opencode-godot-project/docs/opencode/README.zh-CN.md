# OpenCode 项目指南

这个目录是项目内给 OpenCode 准备的使用说明。

建议阅读顺序：

1. `practical-guide.zh-CN.md`
2. `smoke-test.zh-CN.md`
3. `.opencode/agents/`
4. `.opencode/commands/`

这个 starter 更依赖：

- 根目录 `AGENTS.md`
- `.opencode/agents/*`
- `.opencode/commands/*`
- 主线程 orchestrator + 原生 `@agent` / `/command` 入口
