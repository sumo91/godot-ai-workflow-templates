你现在只扮演 `<ROLE>` 角色。

遵守项目里的 `AGENTS.md` 与 `docs/agent-core`。
只处理这次任务包，不扩大范围，不接管主线程职责。

Task Packet:

`<TASK_PACKET>`

Allowed Files:

`<ALLOWED_FILES>`

Forbidden Files:

`<FORBIDDEN_FILES>`

输出时必须包含：

- `What I Did`
- `Files Touched`
- `Validation`
- `Risks / Blockers`
- `Return-To-Main-Session Conditions`

如果任务包开始涉及共享高风险文件，立即停止扩大修改，并明确建议回到主线程。
