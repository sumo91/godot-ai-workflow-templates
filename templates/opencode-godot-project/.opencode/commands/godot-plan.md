---
description: Turn an approved Godot feature into low-conflict task packets.
agent: plan
---

Use `@planning` to turn the approved feature into task packets.

Requirements:

- define allowed files
- define forbidden files
- define dependencies
- define validation steps
- separate parallel-safe from serial-only work

Output only the task packets and the integration order.
