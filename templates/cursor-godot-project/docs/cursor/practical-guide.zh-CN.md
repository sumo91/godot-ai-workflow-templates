# Cursor Practical Guide

最推荐的日常模式是：

1. 用 `.cursor/rules` 保持底层行为一致
2. 用 `.cursor/commands` 作为阶段入口
3. 共享高风险文件回到主会话

优先入口：

- `/feature`
- `/bugfix`
- `/godot-plan`
- `/godot-review`
- `/nightly-prepare`
